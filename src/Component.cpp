#include "Component.h"

// Default implementations are in the header file
// This file exists for potential future implementations

void Component::setObject(Object* object) 
{
    this->object = object;
}